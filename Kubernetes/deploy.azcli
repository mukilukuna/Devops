az login 
az account set --subscription "*********************"

az aks create --resource-group loi --name aks-debock --node-count 1 --enable-addons monitoring --generate-ssh-keys
kubectl apply -f .\webshop.yaml
kubectl get nodes
